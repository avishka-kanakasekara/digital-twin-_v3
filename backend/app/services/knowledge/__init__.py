"""
Knowledge Intelligence Pipeline — Service Package

Modules:
    file_validator       — File security & validation
    text_extractor       — PDF/DOCX/TXT/JSON/CSV extraction
    document_classifier  — Document type classification
    ai_analyzer          — Gemini AI structured extraction
    skill_normalizer     — Skill canonicalization
    reconciliation_engine — Safe data reconciliation
    pipeline             — Pipeline orchestrator
"""
