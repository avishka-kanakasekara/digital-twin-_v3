# Core API
fastapi==0.115.12
uvicorn[standard]==0.34.3
pydantic==2.8.2
pydantic-settings==2.4.0
python-dotenv==1.1.1

# Database — Supabase (hosted PostgreSQL)
supabase==2.15.1

# Auth
python-jose[cryptography]==3.5.0
passlib[bcrypt]==1.7.4
bcrypt==4.0.1

# CORS / HTTP
python-multipart==0.0.20
httpx==0.28.1

# Cache & Queue
redis==5.2.0

# Google Gemini AI — Vertex AI SDK
google-genai>=1.0.0
google-cloud-aiplatform>=1.70.0

# Document extraction
PyPDF2>=3.0.0          # PDF text extraction
python-docx>=1.1.0     # DOCX text extraction

# MIME type detection from magic bytes (optional — graceful fallback if libmagic not installed)
# Requires: brew install libmagic (macOS) or apt-get install libmagic1 (Linux)
python-magic>=0.4.27
