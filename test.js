import('puppeteer').then(async p => { const b = await p.launch(); const page = await b.newPage(); await page.goto('http://localhost:5173/context', {waitUntil: 'networkidle0'}); const tabs = await page.\$\$eval('button', btns => btns.map(b => ({text: b.innerText, rect: JSON.parse(JSON.stringify(b.getBoundingClientRect()))}))); console.log(JSON.stringify(tabs, null, 2)); await b.close(); })
